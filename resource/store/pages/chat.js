import * as user from "../modules/user";
import * as chat from "../modules/chat";
import * as show from "../modules/show";

const stores = {
  modules: {
    user, chat, show
  }
};

export {
  stores
};
